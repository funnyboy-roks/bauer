# Testing

This directory contains a few crates used for testing `bauer`.

For example, the `attribute` crate adds the `pre` attribute that places
tokens before an item so that we can test the `attributes` attribute.
